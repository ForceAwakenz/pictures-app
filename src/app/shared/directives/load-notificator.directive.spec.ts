import { LoadNotificatorDirective } from './load-notificator.directive';

describe('LoadNotificatorDirective', () => {
	it('should create an instance', () => {
		const directive = new LoadNotificatorDirective();
		expect(directive).toBeTruthy();
	});
});
